<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BlueBanquise Overlord</title>
    <!-- Bulma CSS Framework -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.4/css/bulma.min.css">
    
    <style>
        /* Modern Overlord Theme Overrides */
        :root {
            /* DYNAMIC ACCENT COLOR 
               Change this variable to update the UI color across the whole application.
            */
            --overlord-hover-accent: #CCFF00; 

            --overlord-navbar: #323232;
            --overlord-sidebar: #424242;
            --overlord-bg: #323232; 
            --overlord-control-bar: #424242; 
            --overlord-card: #3d4148; 
            --overlord-border: #4a4e56; 
            --overlord-text-main: #e2e8f0;
            --overlord-text-muted: #94a3b8;
        }

        body {
            background-color: var(--overlord-bg);
            color: var(--overlord-text-main);
            min-height: 100vh;
        }

        /* Navbar Styling */
        .navbar.is-dark {
            background-color: var(--overlord-navbar) !important;
            border-bottom: 1px solid #222;
        }

        .navbar-item {
            font-weight: 500;
            text-transform: capitalize;
            color: #eee !important;
        }
        
        .navbar-item.is-active {
            color: var(--overlord-hover-accent) !important;
            background-color: transparent !important;
        }

        /* Sidebar Styling */
        .sidebar-menu {
            background: var(--overlord-sidebar);
            min-height: calc(100vh - 3.25rem);
            padding: 1.5rem 0;
            color: #cbd5e1;
            border-right: 1px solid #333;
        }

        .menu-label {
            color: #888 !important;
            font-size: 0.75rem;
            letter-spacing: 0.05em;
            font-weight: 700;
            text-transform: uppercase;
            margin-bottom: 0.75rem;
            padding: 0 1.5rem;
        }

        .menu-list a {
            color: #bbb;
            background-color: transparent;
            border-radius: 0 !important;
            transition: all 0.2s ease;
            margin-bottom: 0;
            padding: 0.75rem 1.5rem;
        }

        .menu-list a:hover {
            background-color: rgba(255, 255, 255, 0.03) !important;
            color: var(--overlord-hover-accent) !important;
        }

        .menu-list a.is-active {
            background-color: rgba(0, 0, 0, 0.1) !important;
            color: var(--overlord-hover-accent) !important;
            font-weight: 600;
            border-left: 3px solid var(--overlord-hover-accent);
            padding-left: calc(1.5rem - 3px);
        }

        /* Content Area Layout */
        .content-area {
            padding: 0;
            background-color: var(--overlord-bg);
        }

        .content-header {
            padding: 1.5rem 2rem;
        }

        /* Control Bar - Slimmed height and #424242 */
        .control-bar {
            background-color: var(--overlord-control-bar);
            border-bottom: 1px solid #333;
            padding: 0.4rem 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            min-height: 2.5rem;
        }

        .control-bar-label {
            font-size: 0.85rem;
            color: var(--overlord-text-muted);
            font-weight: 500;
        }

        .main-container {
            padding: 2rem;
        }

        .title {
            color: var(--overlord-text-main) !important;
            font-weight: 700;
            letter-spacing: -0.02em;
            margin-bottom: 0.5rem !important;
        }

        .subtitle {
            color: var(--overlord-text-muted) !important;
        }

        /* Updated Box: Lifted slate color to contrast with dark background */
        .box {
            background-color: var(--overlord-card);
            border-radius: 4px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.5);
            border: 1px solid var(--overlord-border);
            color: var(--overlord-text-main);
        }

        .label {
            color: var(--overlord-text-main);
            font-size: 0.875rem;
            font-weight: 600;
        }

        /* Form Inputs */
        .input, .select select {
            background-color: #1a1c23;
            border-color: #444;
            color: white;
            box-shadow: none;
            border-radius: 4px;
        }

        .input::placeholder {
            color: #4b5563;
        }

        .input:focus, .select select:focus {
            border-color: var(--overlord-hover-accent);
            box-shadow: 0 0 0 0.125em rgba(204, 255, 0, 0.1);
            background-color: #1a1c23;
            color: white;
        }

        .select:not(.is-multiple):not(.is-loading)::after {
            border-color: var(--overlord-hover-accent);
        }

        /* Primary Button override using the accent variable */
        .button.is-primary {
            background-color: var(--overlord-hover-accent) !important;
            border-radius: 4px;
            font-weight: 700;
            transition: all 0.2s ease;
            color: #1a1c23 !important;
            border: none !important;
            text-transform: uppercase;
            letter-spacing: 0.025em;
        }

        .button.is-primary:hover {
            filter: brightness(1.1);
            box-shadow: 0 0 15px var(--overlord-hover-accent);
            opacity: 0.9;
        }

        .main-layout {
            display: flex;
            min-height: calc(100vh - 3.25rem);
        }

        @media screen and (max-width: 768px) {
            .main-layout {
                flex-direction: column;
            }
            .sidebar-menu {
                width: 100% !important;
                min-height: auto;
                border-right: none;
                border-bottom: 1px solid var(--overlord-border);
            }
        }
    </style>
</head>
<body>

<script>
    // System Notifications
    window.showNotification = function(type, message) {
        const container = document.getElementById('notification-container');
        const notif = document.createElement('div');
        const themeClass = type === 'is-danger' ? 'is-danger' : 'is-dark';
        notif.className = `notification ${themeClass}`;
        notif.style.pointerEvents = 'auto';
        notif.style.marginBottom = '0.5rem';
        notif.style.borderRadius = '4px';
        notif.style.border = '1px solid #444';
        notif.innerHTML = `
            <button class="delete" onclick="this.parentElement.remove()"></button>
            ${message}
        `;
        container.appendChild(notif);
        setTimeout(() => notif.remove(), 4000);
    };
</script>

<nav class="navbar is-dark" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <a class="navbar-item" href="/">
            <span style="color: var(--overlord-hover-accent); margin-right: 8px;">●</span>
            <strong style="color: white;">Overlord</strong>
        </a>
        <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" onclick="this.classList.toggle('is-active'); document.getElementById('navbarMain').classList.toggle('is-active');">
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
        </a>
    </div>

    <div id="navbarMain" class="navbar-menu">
        <div class="navbar-start">
            <a class="navbar-item is-active" href="/inventory">inventory</a>
        </div>
    </div>
</nav>

<div class="main-layout">
    <!-- Sidebar -->
    <aside class="sidebar-menu" style="width: 260px;">
        <div class="menu">
            <p class="menu-label">group</p>
            <ul class="menu-list">
                <li><a href="/inventory/group/list">List groups</a></li>
                <li><a href="/inventory/group/add" class="is-active">Add new group</a></li>
            </ul>

            <p class="menu-label" style="margin-top: 2rem;">host</p>
            <ul class="menu-list">
                <li><a href="/inventory/host/list">List hosts</a></li>
                <li><a href="/inventory/host/add">Add new host</a></li>
                <li><a href="/inventory/host/delete">Delete an host</a></li>
            </ul>
        </div>
    </aside>

    <!-- Main Content Area -->
    <main class="content-area" style="flex-grow: 1;">
        
        <!-- Control Bar -->
        <div class="control-bar">
            <div class="control-bar-label">
                <span style="color: var(--overlord-hover-accent); font-weight: bold;">INFO</span> 
                <span style="margin-left: 8px;">Groups categorize hosts by function, hardware, or location.</span>
            </div>
            <div class="buttons are-small">
                <button class="button is-dark" style="background: transparent; border: 1px solid #555; color: #ccc;">
                    Sort by: Name
                </button>
            </div>
        </div>

        <!-- Content Header -->
        <header class="content-header">
            <h1 class="title is-4">Create a new group</h1>
            <p class="subtitle is-6">Set up your cluster resources within the inventory management system.</p>
        </header>

        <!-- Main Form Container -->
        <div class="main-container">
            <div class="box" style="max-width: 600px;">
                <div class="field">
                    <label class="label">Group Name</label>
                    <div class="control">
                        <input id="group_name" class="input" type="text" placeholder="e.g. production-database">
                    </div>
                </div>

                <div class="field">
                    <label class="label">Group Type</label>
                    <div class="control">
                        <div class="select is-fullwidth">
                            <select id="group_type">
                                <option value="function">Function group</option>
                                <option value="os">Operating System group</option>
                                <option value="hardware">Hardware group</option>
                                <option value="rack">Rack group</option>
                                <option value="custom">Custom group</option>
                            </select>
                        </div>
                    </div>
                </div>

                <div class="field" style="margin-top: 2rem;">
                    <div class="control">
                        <button class="button is-primary is-fullwidth" onclick="goNext()">
                            <span>Continue</span>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </main>
</div>

<!-- Floating Notifications -->
<div id="notification-container"
     style="
       position: fixed;
       bottom: 2rem;
       right: 2rem;
       width: 320px;
       display: flex;
       flex-direction: column;
       pointer-events: none;
       z-index: 9999;
     ">
</div>

<script>
    function goNext() {
        const name = document.getElementById("group_name").value.trim();
        const type = document.getElementById("group_type").value;

        if (!name) {
            showNotification("is-danger", "Inventory group name cannot be empty.");
            return;
        }

        const targetUrl = `/inventory/group/add/${type}?name=${encodeURIComponent(name)}`;
        showNotification("is-success", `Validating metadata for ${name}...`);
        console.log("Redirecting to:", targetUrl);
    }
</script>

</body>
</html>