name: inventory_host
title: Host Inventory
version: "1.0.0"

ui_integration:
  inventory:
    host:
      - name: List hosts
        url: /inventory/host/list
      - name: Add new host
        url: /inventory/host/add
      - name: Delete an Host
        url: /inventory/host/delete
